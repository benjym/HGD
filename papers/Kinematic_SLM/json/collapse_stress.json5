{
    // resolution
    nx : 200, // width

    // geometry
    H : 1, // (m)
    aspect_ratio_y : 0.5,  // ny = aspect_ratio * nx
    nm : 100,

    // material properties
    repose_angle : 30, // (degrees)
    gsd_mode : 'mono',
    s_m : 0.01, // minimum particle size (m)
    s_M : 0.01, // maximum particle size (m)
    nu_cs : 0.5,
    // alpha : 1,
    
    // initial condition:
    fill_ratio : 0.25,
    nu_fill : 0.5,
    IC_mode : "column",
    
    // saving
    save_inc : 100,   // how many saves to make
    t_f : null,
    
    plot : ['nu', 'stress', 'anisotropy'],
    videos : ['nu','stress', 'anisotropy'],
    save : ['nu', 's','stress', 'last_swap'],
    mask_s_bar : false,

    // parallel
    max_workers : 1, // leave some cores on my poor little laptop
}
